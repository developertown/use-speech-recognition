import * as React from "react";
import { SpeechDisplay } from "../example/speech-display";

export default {
  title: "displaySpokenWords",
  component: SpeechDisplay,
};

export const displaySpokenWords = () => <SpeechDisplay />;
